#!/usr/bin/env ruby

if (ARGV[0] == nil || ARGV[1] == nil)
  puts "Missing Args, expecting repeat_test {int} {string}"
  exit
end

require 'repeat_test'

runner = RepeatTest.new
runner.run(ARGV[0].to_i, ARGV[1])
